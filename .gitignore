# Node dependencies
node_modules/

# Data files (generated at runtime)
employees.json
Astonunion.bin
*.dat

# Compiled C binaries (legacy, not needed for deployment)
*.exe
Uniondatabase
Search
Readuniondatabase

# Admin panel local server (not needed on Render, main server.js handles it)
admin_server.js

# Env variables
.env
.DS_Store
